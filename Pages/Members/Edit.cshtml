@page
@model ProjectGym.Pages.Members.EditModel

@{
    ViewData["Title"] = "Edit Member";
}

<div class="container py-5">
    <h1 class="text-center display-4 mb-4">@ViewData["Title"]</h1>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow">
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="Member.MemberID" />

                        <div class="form-group">
                            <label asp-for="Member.Name" class="control-label font-weight-bold">Name</label>
                            <input asp-for="Member.Name" class="form-control" />
                            <span asp-validation-for="Member.Name" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Member.email" class="control-label font-weight-bold">Email</label>
                            <input asp-for="Member.email" class="form-control" />
                            <span asp-validation-for="Member.email" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Member.phone" class="control-label font-weight-bold">Phone</label>
                            <input asp-for="Member.phone" class="form-control" />
                            <span asp-validation-for="Member.phone" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="Member.address" class="control-label font-weight-bold">Address</label>
                            <input asp-for="Member.address" class="form-control" />
                            <span asp-validation-for="Member.address" class="text-danger"></span>
                        </div>

                        <div class="form-group text-center mt-4">
                            <button type="submit" class="btn btn-primary btn-lg mr-2">Save</button>
                            <a asp-page="./Index" class="btn btn-secondary btn-lg">Back to List</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
